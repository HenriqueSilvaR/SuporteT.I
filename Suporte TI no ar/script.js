// Aguarda o carregamento completo da página
document.addEventListener("DOMContentLoaded", () => {
  // Fazendo a animação de fade-in no conteúdo
  document.querySelector(".content").style.opacity = 1;
});
